<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="android:actionMenuTextAppearance">@style/MyMenuTextStyle</item>
        <item name="android:textSize">12sp</item>
        <item name="android:windowEnterTransition">@transition/anim_duration</item>
        <item name="android:windowExitTransition">@transition/anim_duration</item>
        <item name="android:listDivider">@drawable/grid_line</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!--<item name="android:windowAnimationStyle">@style/activityAnim</item>-->
    </style>

    <!-- 使用style方式定义activity切换动画 -->
    <style name="activityAnim">
        <!--打开新的Activity并进入新的Activity展示的动画-->
        <item name="android:activityOpenEnterAnimation">@anim/open_activity_rightgolift</item>
        <!--打开新的Activity并销毁之前的Activity展示的动画-->
        <item name="android:activityOpenExitAnimation">@anim/open_activity_rightgolift</item>
        <!--关闭当前Activity进入上一个Activity展示的动画-->
        <item name="android:activityCloseEnterAnimation">@anim/open_activity_up</item>
        <!--关闭当前Activity时展示的动画-->
        <item name="android:activityCloseExitAnimation">@anim/open_activity_up</item>
    </style>

    <!--全部最大-->
    <style name="MatchMatch">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <!--顶部消失，layout顶到头-->
    <style name="TransparencyTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <!--底部操作栏透明-->
        <!-- <item name="android:windowTranslucentNavigation">true</item>-->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="OverflowMenuStyle" parent="@style/ThemeOverlay.AppCompat">
        <item name="overlapAnchor">false</item>
        <item name="android:colorBackground">@color/cstore_white</item>
    </style>


    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="MyMenuTextStyle">
        <item name="android:textSize">16sp</item>
    </style>

    <!--提示框字体大小设置-->
    <style name="AlertDialogCustom" parent="Animation.AppCompat.Dialog">
        <item name="android:textSize">14sp</item>
    </style>

</resources>
